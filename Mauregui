<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Mauregui Automotores | Tu próximo auto te espera</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Maurregui Automotores. Encontrá tu 0km o usado seleccionado con financiación, toma de usados y asesoramiento personalizado." />

  <!-- Fuente Google -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

  <style>
    :root {
      --color-primario: #0b3c6f;   /* Azul Maurregui (a gusto) */
      --color-secundario: #f5b301; /* Amarillo/acento */
      --color-texto: #222;
      --color-texto-suave: #555;
      --bg-claro: #f7f7f7;
      --fuente: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: var(--fuente);
      color: var(--color-texto);
      background-color: #fff;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      max-width: 100%;
      display: block;
    }

    /* HEADER */
    .site-header {
      position: sticky;
      top: 0;
      z-index: 50;
      background: #000;
      color: #fff;
      padding: 10px 20px;
    }

    .header-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      flex-wrap: wrap;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 700;
      font-size: 1.1rem;
    }

    .logo-mark {
      width: 36px;
      height: 36px;
      border-radius: 8px;
      background: linear-gradient(135deg, var(--color-primario), #041f36);
    }

    .main-nav {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      font-size: 0.95rem;
    }

    .main-nav a {
      color: #fff;
      font-weight: 500;
    }

    .main-nav a:hover {
      text-decoration: underline;
    }

    .cta-whatsapp {
      background: #25d366;
      color: #111;
      padding: 8px 14px;
      border-radius: 999px;
      font-size: 0.9rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    .cta-whatsapp span {
      font-size: 1.1rem;
    }

    /* HERO */
    .hero {
      position: relative;
      min-height: min(80vh, 720px);
      background: radial-gradient(circle at 0 0, #0f609b 0, #020b16 48%, #000 100%);
      color: #fff;
      overflow: hidden;
    }

    .hero-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px 60px;
      display: grid;
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
      gap: 30px;
      align-items: center;
    }

    .hero-kicker {
      text-transform: uppercase;
      letter-spacing: 0.14em;
      font-size: 0.75rem;
      color: #d0e7ff;
      margin-bottom: 10px;
      font-weight: 600;
    }

    .hero-title {
      font-size: clamp(2rem, 4vw, 3rem);
      line-height: 1.05;
      margin: 0 0 14px;
      font-weight: 800;
    }

    .hero-title span {
      color: var(--color-secundario);
    }

    .hero-subtitle {
      margin: 0 0 18px;
      font-size: 0.98rem;
      max-width: 36rem;
      color: #e4eef8;
    }

    .hero-badges {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 22px;
      font-size: 0.8rem;
    }

    .badge {
      padding: 6px 10px;
      border-radius: 999px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.18);
      backdrop-filter: blur(8px);
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      padding: 10px 18px;
      border-radius: 999px;
      border: none;
      font-weight: 700;
      font-size: 0.95rem;
      background: #fff;
      color: #111;
      cursor: pointer;
      box-shadow: 0 16px 38px rgba(0,0,0,.45);
    }

    .btn-secondary {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      padding: 10px 16px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.35);
      font-weight: 600;
      font-size: 0.9rem;
      background: transparent;
      color: #fff;
      cursor: pointer;
    }

    .hero-image-wrap {
      position: relative;
    }

    .hero-car {
      border-radius: 16px;
      overflow: hidden;
      background: radial-gradient(circle at top, #ffffff 0, #c7d5e6 40%, #0f2640 100%);
      padding: 20px;
      box-shadow: 0 24px 60px rgba(0,0,0,.55);
    }

    .hero-car img {
      border-radius: 10px;
      object-fit: contain;
      max-height: 260px;
      margin: 0 auto;
    }

    .hero-label {
      position: absolute;
      right: 0;
      bottom: -18px;
      transform: translateY(50%);
      background: #fff;
      color: #111;
      padding: 10px 16px;
      border-radius: 999px;
      font-size: 0.8rem;
      font-weight: 600;
      box-shadow: 0 20px 40px rgba(0,0,0,.45);
    }

    @media (max-width: 900px) {
      .hero-inner {
        grid-template-columns: minmax(0, 1fr);
      }
      .hero-image-wrap {
        order: -1;
      }
      .hero-label {
        right: 20px;
      }
    }

    @media (max-width: 600px) {
      .hero {
        min-height: auto;
      }
      .hero-inner {
        padding-top: 24px;
      }
    }

    /* STATS */
    .stats-bar {
      background-color: var(--bg-claro);
      padding: 24px 20px;
    }

    .stats-grid {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 18px;
    }

    .stat-item {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .stat-icon {
      width: 38px;
      height: 38px;
      border-radius: 12px;
      background: rgba(11, 60, 111, 0.09);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
    }

    .stat-text-main {
      font-weight: 600;
      font-size: 0.95rem;
    }

    .stat-text-sub {
      font-size: 0.85rem;
      color: var(--color-texto-suave);
    }

    /* SECCIÓN GENÉRICA */
    section {
      padding: 40px 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .section-title {
      font-size: 1.6rem;
      margin: 0 0 8px;
    }

    .section-subtitle {
      margin: 0 0 24px;
      color: var(--color-texto-suave);
      font-size: 0.95rem;
    }

    /* MARCAS */
    .brands-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 20px;
      align-items: center;
    }

    .brand-card {
      background: #fff;
      border-radius: 12px;
      padding: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid #eee;
      min-height: 70px;
      font-weight: 600;
      font-size: 0.9rem;
    }

    /* CATEGORÍAS */
    .categories-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
      gap: 16px;
    }

    .category-card {
      background: #fff;
      border-radius: 12px;
      padding: 14px 12px;
      text-align: center;
      border: 1px solid #eee;
      cursor: pointer;
      transition: transform 0.12s ease, box-shadow 0.12s ease, border-color 0.12s ease;
      font-size: 0.9rem;
      font-weight: 500;
    }

    .category-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 24px rgba(0,0,0,.06);
      border-color: var(--color-primario);
    }

    /* AUTO DESTACADO */
    .featured {
      background: linear-gradient(135deg, #fdfdfd, #f3f6fb);
    }

    .featured-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
      gap: 26px;
      align-items: center;
    }

    .featured-kicker {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      color: var(--color-secundario);
      margin-bottom: 6px;
      font-weight: 600;
    }

    .featured-title {
      margin: 0 0 10px;
      font-size: 1.4rem;
    }

    .featured-desc {
      margin: 0 0 16px;
      font-size: 0.95rem;
      color: var(--color-texto-suave);
    }

    .pill-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 18px;
      font-size: 0.8rem;
    }

    .pill {
      padding: 6px 10px;
      border-radius: 999px;
      background: #fff;
      border: 1px solid #e1e5ed;
    }

    .featured-img-wrap {
      border-radius: 18px;
      background: radial-gradient(circle at top, #ffffff 0, #d8e0f0 40%, #c0c8d8 100%);
      padding: 20px;
      box-shadow: 0 24px 48px rgba(0,0,0,.18);
    }

    /* QUIÉNES SOMOS */
    .about {
      background: #f9f9f9;
    }

    .about p {
      font-size: 0.95rem;
      line-height: 1.7;
      color: var(--color-texto-suave);
      margin-bottom: 12px;
    }

    /* SUCURSALES / CTA */
    .branches {
      position: relative;
      color: #fff;
      overflow: hidden;
    }

    .branches::before {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at 0 0, #2f7ac1 0, #0b3c6f 40%, #020913 100%);
      opacity: 0.95;
      z-index: -2;
    }

    .branches-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, .9fr);
      gap: 24px;
      align-items: center;
    }

    .branches-card {
      background: rgba(0,0,0,0.26);
      border-radius: 16px;
      padding: 18px 18px 16px;
      font-size: 0.9rem;
      border: 1px solid rgba(255,255,255,0.18);
    }

    .branches-card h3 {
      margin-top: 0;
      margin-bottom: 6px;
    }

    .branches-list {
      margin: 0;
      padding-left: 18px;
      font-size: 0.9rem;
    }

    .branches-list li {
      margin-bottom: 4px;
    }

    /* FOOTER */
    .site-footer {
      background: #000;
      color: #eee;
      padding: 26px 20px;
      font-size: 0.85rem;
    }

    .footer-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      flex-wrap: wrap;
      gap: 18px;
      align-items: center;
      justify-content: space-between;
    }

    .footer-links {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header class="site-header">
    <div class="header-inner">
      <a href="#inicio" class="logo">
        <div class="logo-mark"></div>
        <div>
          Maurregui<br />
          <span style="font-size:0.8rem; font-weight:400;">Automotores</span>
        </div>
      </a>
      <nav class="main-nav">
        <a href="#inicio">Inicio</a>
        <a href="#modelos">Modelos</a>
        <a href="#categorias">Categorías</a>
        <a href="#sucursales">Sucursales</a>
        <a href="#contacto">Contacto</a>
        <a href="https://wa.me/5491112345678" target="_blank" class="cta-whatsapp">
          <span>💬</span> WhatsApp
        </a>
      </nav>
    </div>
  </header>

  <!-- HERO -->
  <section id="inicio" class="hero">
    <div class="hero-inner">
      <div>
        <div class="hero-kicker">Maurregui Automotores</div>
        <h1 class="hero-title">
          Tu próximo auto, <span>más cerca</span> de lo que pensás.
        </h1>
        <p class="hero-subtitle">
          0km y usados seleccionados, financiación a medida, toma de usados y un equipo que te acompaña en todo el proceso de compra.
        </p>
        <div class="hero-badges">
          <div class="badge">Financiación flexible</div>
          <div class="badge">Toma de usados</div>
          <div class="badge">Asesoramiento personalizado</div>
        </div>
        <div class="hero-actions">
          <a href="#modelos" class="btn-primary">Ver modelos disponibles →</a>
          <a href="#contacto" class="btn-secondary">Quiero que me contacten</a>
        </div>
      </div>
      <div class="hero-image-wrap">
        <div class="hero-car">
          <!-- Reemplazar por foto real -->
          <img src="https://images.pexels.com/photos/210019/pexels-photo-210019.jpeg?auto=compress&cs=tinysrgb&w=800" alt="Auto destacado Maurregui" />
        </div>
        <div class="hero-label">
          Ejemplo de SUV 0km · Desde $X/mes*
        </div>
      </div>
    </div>
  </section>

  <!-- STATS -->
  <section class="stats-bar">
    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-icon">🚗</div>
        <div>
          <div class="stat-text-main">Decenas de unidades en stock</div>
          <div class="stat-text-sub">0km y usados seleccionados</div>
        </div>
      </div>
      <div class="stat-item">
        <div class="stat-icon">🤝</div>
        <div>
          <div class="stat-text-main">Planes de financiación a medida</div>
          <div class="stat-text-sub">Trabajamos con bancos y financieras</div>
        </div>
      </div>
      <div class="stat-item">
        <div class="stat-icon">📍</div>
        <div>
          <div class="stat-text-main">Presencia en la zona</div>
          <div class="stat-text-sub">Sucursales cercanas para entregar tu unidad</div>
        </div>
      </div>
      <div class="stat-item">
        <div class="stat-icon">⭐</div>
        <div>
          <div class="stat-text-main">Acompañamiento post-venta</div>
          <div class="stat-text-sub">Te asesoramos incluso después de la compra</div>
        </div>
      </div>
    </div>
  </section>

  <!-- MARCAS -->
  <section id="modelos">
    <div class="container">
      <h2 class="section-title">Marcas con las que trabajamos</h2>
      <p class="section-subtitle">
        Podés adaptar esta sección a las marcas reales de Maurregui: Fiat, Renault, Peugeot, etc. Aquí es solo un ejemplo visual.
      </p>
      <div class="brands-grid">
        <div class="brand-card">Fiat</div>
        <div class="brand-card">Renault</div>
        <div class="brand-card">Peugeot</div>
        <div class="brand-card">Chevrolet</div>
        <div class="brand-card">Toyota</div>
        <div class="brand-card">Volkswagen</div>
      </div>
    </div>
  </section>

  <!-- CATEGORÍAS -->
  <section id="categorias">
    <div class="container">
      <h2 class="section-title">Buscar por tipo de vehículo</h2>
      <p class="section-subtitle">
        Atajos rápidos para que el usuario llegue más fácil a lo que le interesa.
      </p>
      <div class="categories-grid">
        <div class="category-card">SUV</div>
        <div class="category-card">Pick-up</div>
        <div class="category-card">Sedán</div>
        <div class="category-card">Hatchback</div>
        <div class="category-card">Familiares</div>
        <div class="category-card">Utilitarios</div>
      </div>
    </div>
  </section>

  <!-- AUTO DESTACADO -->
  <section class="featured">
    <div class="container featured-grid">
      <div>
        <div class="featured-kicker">Unidad destacada</div>
        <h3 class="featured-title">Ejemplo: SUV 0km Línea 2025</h3>
        <p class="featured-desc">
          Podés usar esta sección para mostrar una oferta puntual: un modelo nuevo, una promo de financiación o una unidad con entrega inmediata.
        </p
